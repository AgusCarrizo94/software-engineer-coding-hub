{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/Build Docker Image/package.json",
                "localRoot": "${workspaceFolder}/Build Docker Image"
            }
        }
    ]
}